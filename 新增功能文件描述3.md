# 新增功能文件描述 3：一次开发多端部署 + 自由流转

---

## 功能一：一次开发，多端部署（简称"一多"）

### 1. 功能定义与目标

本项目采用 HarmonyOS **"一次开发，多端部署"** 架构规范，通过**一套代码工程、一次开发上架**，实现应用在**小设备（手机）、中设备（折叠屏）和大设备（平板、PC）** 上的按需部署与多端适配。

### 2. 界面级适配架构（UI 适配实现）

#### 2.1 自适应布局（区域局部连续变化）

项目中通过合理利用以下容器和能力实现页面各区域内的局部差异适配：

| 容器/能力 | 使用位置 | 说明 |
|-----------|----------|------|
| **Row / Column** | `Index.ets` 所有布局函数 | 使用 `layoutWeight(1)` 实现等比拉伸 |
| **Flex / FlexAlign** | `PhoneIndex.ets` 底部标签栏 | `FlexAlign.SpaceAround` 实现均分布局 |
| **List** | `FileListView.ets` 文件列表 | 垂直滚动列表，自动延伸 |
| **Scroll** | `Index.ets` 所有标签页 | 内容区域滚动，解决小屏溢出问题 |
| **layoutWeight** | `renderFilesTab`、`renderPreviewTab` | 比重分配，文件列表区域占满剩余空间 |
| **Blank** | 导航栏、设备选择器 | 空白填充占位 |

#### 2.2 响应式布局（整体结构条件触发）

##### 断点系统（BreakpointSystem）

在 `common/utils/BreakpointSystem.ets` 中实现了**五档断点标准**：

| 断点 | 宽度范围(vp) | 适用设备 | 栅格列数 |
|------|-------------|---------|---------|
| **XS** | < 320 | 小型手表/折叠屏折叠态 | 4 列 |
| **SM** | 320 ~ 599 | 常规手机竖屏 | 4 列 |
| **MD** | 600 ~ 839 | 折叠屏展开态/小平板 | 8 列 |
| **LG** | 840 ~ 1079 | 平板横屏/小 PC | 12 列 |
| **XL** | ≥ 1080 | PC/大屏幕设备 | 12 列 |

断点系统采用**单例模式**，通过 `display.getDefaultDisplaySync()` 获取初始窗口宽度，在 `Index.ets` 的 `aboutToAppear()` 中初始化。监听器模式实现断点变化通知：

```typescript
// BreakpointSystem.ets - 核心接口
public updateWindowWidth(widthVp: number): void;   // 更新窗口尺寸
public getCurrentBreakpoint(): BreakpointType;       // 获取当前断点
public isSmallScreen(): boolean;                     // XS / SM
public isMediumScreen(): boolean;                    // MD
public isLargeScreen(): boolean;                     // LG / XL
public getGridColumns(): number;                     // 获取当前栅格列数
```

在 `Index.ets` 的 `initBreakpointSystem()` 方法中，通过添加监听器实现断点变化时自动更新 UI：

```typescript
this.breakpointSystem.addListener({
  onBreakpointChanged: (breakpoint, windowWidth) => {
    this.currentBreakpoint = breakpoint;
    this.windowWidth = windowWidth;
    this.gridColumns = this.breakpointSystem.getGridColumns();
    this.updateSwiperDisplayCount();
    this.updateLayoutForBreakpoint(breakpoint);
  }
});
```

##### 栅格系统（GridRow / GridCol）

在 `common/utils/GridLayout.ets` 中实现了响应式栅格封装：

- **GridLayout 组件**：封装 `GridRow`，支持配置 `columns` 列数和 `gutter` 间距
- **ResponsiveGridCol（别名 GridCol）**：封装 `GridCol`，根据当前断点自动匹配对应的 `span` 和 `offset` 配置
- **ResponsiveUtils 工具类**：提供 `getValueByBreakpoint()` 方法，根据断点获取响应式值

栅格列数配置：
- **XS/SM**：4 列（窄网格，适合手机）
- **MD**：8 列（中等网格，适合折叠屏）
- **LG/XL**：12 列（宽网格，适合平板/PC）

##### 核心组件一多响应实现

**（1）Tabs 标签栏**

在 `Index.ets` 中，标签栏根据断点和屏幕方向自动调整：
- **小屏竖屏（SM/MD）**：底部水平标签栏（通过 `renderBottomTabBar` 或 `Tabs` 组件实现）
- **大屏/横屏（LG/XL/Portrait）**：左侧垂直标签栏，右侧内容区域

```typescript
// Index.ets 中 updateLayoutForBreakpoint
private updateLayoutForBreakpoint(breakpoint: BreakpointType): void {
  this.showSideBar = (breakpoint === BreakpointType.LG || breakpoint === BreakpointType.XL);
}
```

在 `PhoneIndex.ets` 中实现底部标签栏（Phone 产品特征）：
- 使用 `Row` + `ForEach` 渲染标签项
- 选中的标签高亮显示

**（2）Swiper 轮播**

在 `Index.ets` 中通过断点动态调节 `displayCount`：

```typescript
private displayCountConfig: Record<string, number> = {
  'xs': 1, 'sm': 1, 'md': 2, 'lg': 3, 'xl': 3
};
```

- **手机（XS/SM）**：1 张全宽显示
- **折叠屏（MD）**：2 张并排显示
- **平板/PC（LG/XL）**：3 张并排显示

在 `common/utils/ResponsiveSwiper.ets` 中还封装了可复用的 `ResponsiveSwiper` 组件，支持通过 `displayCountConfig` 配置各断点下的显示数量。

**（3）横竖屏适配**

通过 `common/utils/ScreenOrientation.ets` 中的 `ScreenOrientationHelper` 类实现多路径横竖屏检测：

- **路径1**：`mediaquery.matchMediaSync('(orientation: landscape)')` - 标准 API
- **路径2**：`display.getDefaultDisplaySync().orientation` - 快速读取
- **路径3**：`display.on('change')` - 显示变化监听

在 `Index.ets` 中，横竖屏切换触发页面布局重构：
- **竖屏**：单栏流式布局，组件从上到下排列
- **横屏（小屏）**：左右分栏布局，左侧 40% 预览区 + 右侧 60% 文件列表

```typescript
// Index.ets - renderFilesTab
if (this.isLandscape && this.breakpointSystem.isSmallScreen()) {
  // 横屏：左右分栏
  Row() {
    Scroll(...) { /* 预览区 */ }.width('40%')
    FileListView().layoutWeight(1)  /* 文件列表 */
  }
} else {
  // 竖屏/中屏以上：单栏流式
  Scroll(...) { /* 轮播 + 文件列表 */ }
}
```

滚动位置通过 `ScrollerStateManager` 在横竖屏切换时保存和恢复。

### 3. 功能级适配（SysCap 系统能力集机制）

在 `common/utils/SysCapChecker.ets` 中实现了完整的 SysCap 能力检查系统：

#### 支持的能力枚举

```typescript
export enum SystemCapability {
  FILE_MANAGEMENT = 'SystemCapability.FileManagement.FileManager',
  DISTRIBUTED_DATA = 'SystemCapability.DistributedDataManager.DataShare',
  DISTRIBUTED_HARDWARE = 'SystemCapability.DistributedHardware.DeviceManager',
  NFC = 'SystemCapability.Communication.NFC.Core',
  BLUETOOTH = 'SystemCapability.Communication.Bluetooth.Core',
  LOCATION = 'SystemCapability.Location.Location.Core',
  CAMERA = 'SystemCapability.Multimedia.Camera.Core',
  AUDIO = 'SystemCapability.Multimedia.Audio.Core',
  NETWORK = 'SystemCapability.Communication.NetManager',
  SENSOR = 'SystemCapability.Sensors.Sensor',
  DISPLAY = 'SystemCapability.WindowManager.WindowManager.Core',
  NOTIFICATION = 'SystemCapability.Notification.Notification',
  ACCOUNT = 'SystemCapability.Account.AppAccount',
  SECURITY = 'SystemCapability.Security.Capability'
}
```

#### 降级策略

每个能力检测不通过时，提供明确的降级方案：

| 能力 | 降级策略 |
|------|---------|
| 文件管理 | 使用应用沙箱目录替代公共目录 |
| 分布式数据 | 使用本地存储替代分布式数据同步 |
| 分布式硬件 | 禁用跨设备投屏功能 |
| NFC | 使用二维码扫描替代 NFC |
| 蓝牙 | 使用 Wi-Fi 直连替代蓝牙 |
| 定位 | 使用 IP 地址定位或手动输入位置 |
| 相机 | 使用相册选择图片替代相机拍摄 |
| 网络 | 使用离线模式或缓存数据 |

#### 条件渲染组件

`CapabilityConditional` 组件根据能力检查结果渲染不同的 UI 内容：

```typescript
// SysCapChecker.ets
export struct CapabilityConditional {
  // 根据 capability 检查结果，渲染 supportedContent 或 fallbackContent
}
```

#### 在 Index 页面中的使用

```typescript
// Index.ets - aboutToAppear 中检查设备能力
const fr = await this.sysCapChecker.checkCapability(SystemCapability.FILE_MANAGEMENT);
if (!fr.supported) { promptAction.showToast({ message: '文件管理能力受限' }) }
const dr = await this.sysCapChecker.checkCapability(SystemCapability.DISTRIBUTED_HARDWARE);
if (!dr.supported) { console.warn('不支持分布式能力') }
```

### 4. 工程级架构规范（三层架构）

项目严格遵循 HarmonyOS 标准三层工程结构：

```
entry/src/main/ets/
├── common/           ← 公共能力层
├── features/         ← 基础特性层
└── products/         ← 产品定制层
```

#### common（公共能力层）

存放公共 UI 组件、工具库、数据管理等，**只可被上层依赖，严禁反向依赖**。

| 文件 | 功能 |
|------|------|
| `BreakpointSystem.ets` | 断点系统单例工具类 |
| `GridLayout.ets` | 栅格布局组件 + 响应式工具类 |
| `ResponsiveSwiper.ets` | 响应式轮播组件 |
| `ResponsiveTabs.ets` | 响应式标签页组件 |
| `ScreenOrientation.ets` | 屏幕方向适配系统 |
| `SysCapChecker.ets` | 系统能力检查器 |

#### features（基础特性层）

提供功能独立的业务模块（UI 组件和基础服务），**可横向调用 common，被 products 依赖，严禁反向依赖 products**。

| 模块 | 文件 | 功能 |
|------|------|------|
| **filemanager** | `FileList.ets` | 文件列表工具函数 |
| | `FileInfoDialog.ets` | 文件详情对话框 |
| | `FileListView.ets` | 文件列表视图组件 |
| **cast** | `CastService.ets` | 投屏服务单例 |
| | `DevicePicker.ets` | 设备选择器组件 |
| **continuation** | `ContinuationService.ets` | 流转核心服务 |
| | `ContinuationStateModel.ets` | 流转状态模型 |
| | `ContinuationAssetHelper.ets` | 文件资产迁移辅助类 |

#### products（产品定制层）

针对特定设备形态的个性化配置和独立 HAP 入口：

| 产品 | 入口文件 | 特征 |
|------|---------|------|
| **phone** | `products/phone/pages/Index.ets` | 底部标签导航、紧凑 UI |
| **tablet** | `products/tablet/pages/`（可扩展） | - |
| **2in1** | `products/2in1/pages/`（可扩展） | - |

---

## 功能二：自由流转（跨端迁移 / 应用接续）

### 1. 功能定义与目标

当用户在源端设备（如手机）上操作应用时，可通过流转将当前的行为或任务**连续不中断地迁移**到对端设备（如平板、PC）上继续执行，确保业务连续性。

### 2. 基础约束与准备条件

#### 运行环境

- HarmonyOS NEXT Release 及以上版本的双端设备
- API 12+（SDK 5.0.0）

#### 前置条件

- 双端设备需登录同一华为账号
- 同时开启 Wi-Fi 和蓝牙

#### 权限与配置

在 `entry/src/main/module.json5` 中进行了完整配置：

```json5
{
  // 分布式权限声明
  "requestPermissions": [
    {"name": "ohos.permission.DISTRIBUTED_DATASYNC"},
    {"name": "ohos.permission.ACCESS_SERVICE_DM"}
  ],
  "abilities": [
    {
      "name": "EntryAbility",
      "continuable": true,      // ← 关键：启用流转能力
      "orientation": "auto_rotation"
    }
  ]
}
```

**注意**：API 12+ 环境下，`DISTRIBUTED_DATASYNC` 权限无需手动申请（系统自动授予），但为兼容旧版本仍保留声明。

### 3. 跨端迁移运作机制与代码实现

#### 步骤 A：源端发起与状态数据保存（onContinue）

在 `EntryAbility.ets` 的 `onContinue(wantParam)` 方法中实现状态保存：

```typescript
// EntryAbility.ets
onContinue(wantParam: Record<string, Object>): AbilityConstant.OnContinueResult {
  // 1. 版本兼容性检测
  const targetVersion = wantParam['version'] as number | undefined;
  if (targetVersion !== undefined && targetVersion < minimumVersion) {
    return AbilityConstant.OnContinueResult.MISMATCH;
  }

  // 2. 从 AppStorage 收集当前应用状态
  const currentState = ContinuationStateModel.collectFromAppStorage();

  // 3. 轻量级数据直接写入 wantParam（<100KB）
  wantParam['selectedTabIndex'] = currentState.selectedTabIndex;
  wantParam['currentFilePath'] = currentState.currentFilePath;
  wantParam['currentBreakpoint'] = currentState.currentBreakpoint;
  wantParam['windowWidth'] = currentState.windowWidth;
  wantParam['continuationTimestamp'] = Date.now();

  // 4. 返回 AGREE 同意迁移
  return AbilityConstant.OnContinueResult.AGREE;
}
```

**对于大数据量场景**，通过 `ContinuationService.saveStateForContinuation()` 使用分布式数据对象：

```typescript
// ContinuationService.ets - 分布式数据对象保存
public async saveStateForContinuation(
  wantParam: Record<string, Object>,
  state: ContinuationAppState,
  callback: ContinuationResultCallback
): Promise<void> {
  // A.1 创建分布式数据对象
  this.distributedObject = distributedDataObject.create(this.uiAbilityContext, state);

  // A.2 生成组网 ID 并激活
  this.sessionId = distributedDataObject.genSessionId();
  wantParam['dataSessionId'] = this.sessionId;
  this.distributedObject.setSessionId(this.sessionId);

  // A.3 持久化保存到目标设备
  await this.distributedObject.save(this.targetDeviceId);
}
```

#### 步骤 B：文件与媒体资产迁移

在 `ContinuationAssetHelper.ets` 中实现文件资产的跨端迁移：

```typescript
// ContinuationAssetHelper.ets
public static copyFileToDistributedDir(
  distributedDir: string,   // context.distributedFilesDir
  sourceFilePath: string,   // 源文件路径
  fileName?: string
): AssetFileInfo | null {
  // 1. 复制文件到分布式文件目录
  // 2. 获取文件状态（ctime, mtime, size）
  // 3. 构建 commonType.Asset 对象
  const asset: commonType.Asset = {
    name: srcFileName,
    uri: distributedUri,
    path: distributedPath,
    createTime: stat.ctime.toString(),
    modifyTime: stat.mtime.toString(),
    size: stat.size.toString()
  };
  return { originalPath, distributedPath, distributedUri, asset };
}
```

Asset 对象作为分布式数据对象的根属性，随数据对象一起持久化。对端恢复时通过 `createEmptyAsset()` 创建空属性对象，防止数据覆盖。

#### 步骤 C：对端接收与状态恢复

对端设备通过 `EntryAbility` 的 `onCreate()`（冷启动）或 `onNewWant()`（热启动）接收流转数据：

```typescript
// EntryAbility.ets - 冷启动
onCreate(want: Want, launchParam: AbilityConstant.LaunchParam): void {
  if (launchParam.launchReason === AbilityConstant.LaunchReason.CONTINUATION) {
    // 设置迁移状态为 ACTIVE，确保可再次迁移
    this.context.setMissionContinueState(AbilityConstant.ContinueState.ACTIVE, ...);
    // 恢复迁移数据
    this.restoreContinuationData(want);
  }
}

// EntryAbility.ets - 热启动
onNewWant(want: Want, launchParam: AbilityConstant.LaunchParam): void {
  if (launchParam.launchReason === AbilityConstant.LaunchReason.CONTINUATION) {
    this.restoreContinuationData(want);
    this.context.restoreWindowStage(this.storage); // 触发窗口恢复
  }
}
```

**恢复流程**（`restoreContinuationData` 方法）：

```typescript
private restoreContinuationData(want: Want): void {
  const params = want.parameters;

  // 1. 恢复标签页索引
  const tabIndex = params['selectedTabIndex'] as number | undefined;
  AppStorage.setOrCreate('selectedTabIndex', tabIndex);
  AppStorage.setOrCreate('continuationRestoredTabIndex', tabIndex);

  // 2. 恢复文件路径
  const filePath = params['currentFilePath'] as string | undefined;
  AppStorage.setOrCreate('currentFilePath', filePath);
  AppStorage.setOrCreate('continuationRestoredFilePath', filePath);

  // 3. 恢复断点和窗口信息
  AppStorage.setOrCreate('currentBreakpoint', breakpoint);
  AppStorage.setOrCreate('windowWidth', windowWidth);

  // 4. 标记已接续恢复
  AppStorage.setOrCreate('isContinuationRestored', true);
}
```

**窗口恢复**（迁移场景走 `onWindowStageRestore` 而非 `onWindowStageCreate`）：

```typescript
onWindowStageRestore(windowStage: window.WindowStage): void {
  windowStage.loadContent('pages/Index', this.storage, ...);
}
```

### 4. 页面流转状态恢复

在 `Index.ets`（主入口）和 `PhoneIndex.ets`（Phone 产品入口）中，通过 `@StorageLink` 监听恢复标记：

```typescript
// Index.ets
@StorageLink('isContinuationRestored') isContinuationRestored: boolean = false;
@StorageLink('continuationRestoredTabIndex') continuationTabIndex: number = 0;

private checkContinuationRestore(): void {
  if (this.isContinuationRestored && this.continuationTabIndex !== undefined) {
    // 自动切换到源端设备的标签页
    this.selectedTabIndex = this.continuationTabIndex;
    promptAction.showToast({ message: '已从其他设备恢复状态' });
    // 清除恢复标记（仅首次生效）
    AppStorage.setOrCreate('isContinuationRestored', false);
  }
}
```

### 5. 虚拟机模拟演示

由于虚拟机不支持真实的分布式迁移，在设置页面中提供了**模拟流转面板**，便于开发和测试：

| 按钮 | 功能 | 实现方法 |
|------|------|---------|
| **💾 模拟保存（源端）** | 模拟 onContinue 保存状态 | 将当前 `selectedTabIndex`、`currentBreakpoint`、`windowWidth` 等状态写入 AppStorage 的 `continuation_*` 键 |
| **📥 模拟恢复（对端）** | 模拟 onCreate 恢复状态 | 从 AppStorage 读取已保存的 `continuation_*` 数据，恢复标签页和 UI 状态 |

```typescript
// Index.ets - 模拟保存
private simulateContinuationSave(): void {
  AppStorage.setOrCreate('continuation_selectedTabIndex', this.selectedTabIndex);
  AppStorage.setOrCreate('continuation_currentBreakpoint', this.currentBreakpoint);
  // ...显示保存成功提示
}

// Index.ets - 模拟恢复
private simulateContinuationRestore(): void {
  const savedTab = AppStorage.get<number>('continuation_selectedTabIndex');
  if (savedTab !== undefined) {
    this.selectedTabIndex = savedTab;
  }
  // ...显示恢复成功提示
}
```

### 6. 关键约束与注意事项

1. **分布式数据对象大小**：不超过 150KB（跨端迁移场景）
2. **SessionId 命名**：`wantParam` 中的 `"sessionId"` 字段被系统占用，使用 `"dataSessionId"` 作为自定义 key
3. **对端恢复**：属性必须置为 `undefined` 或空字符串，否则会覆盖源端数据
4. **先激活再持久化**：必须在 `setSessionId()` 之后调用 `save()`（先激活再持久化）
5. **先监听再激活**：对端必须先注册 `on('status')` 监听，再调用 `setSessionId()`（防止错过 `restored` 事件）
6. **类成员变量**：分布式数据对象必须使用类成员变量，防止异步回调时临时变量被释放
7. **迁移窗口恢复**：迁移启动时走 `onWindowStageRestore()`，不走 `onWindowStageCreate()`
8. **AppStorage 同步**：tab 切换时自动同步状态到 AppStorage（`handleTabChange` 中调用 `AppStorage.setOrCreate('selectedTabIndex', index)`），供 onContinue 收集
