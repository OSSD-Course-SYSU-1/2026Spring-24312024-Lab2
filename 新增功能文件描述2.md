# 新增功能：文件信息详情预览

## 功能概述

在原有文件预览功能基础上，新增了**文件信息详情预览**功能。该功能在用户单击文件准备预览之前，先弹出一个模态对话框，集中展示该文件的详细元数据，帮助用户在打开前了解文件属性。该功能作为文件预览的前置辅助功能，不与系统预览冲突，旨在提升用户对文件的感知和操作体验。

## 功能特性

### 1. 触发方式
- **单击文件触发**：在文件列表浏览界面，单击任意文件（非文件夹）会弹出文件信息详情对话框
- **长按菜单触发**：长按文件触发上下文菜单，选择"详情"选项可查看文件信息
- **多选模式集成**：在多选模式下，选中文件后点击"详情"按钮可查看第一个选中文件的详情
- **批量预览前置**：在批量预览流程中，每个文件预览前都会显示其详细信息

### 2. 对话框内容
文件信息详情对话框展示以下完整元数据：

| 字段 | 说明 | 数据来源 |
|------|------|----------|
| **文件名** | 文件的名称（不含路径） | 从文件路径提取 |
| **完整路径** | 文件的绝对路径 | 文件路径参数 |
| **文件大小** | 自动转换为 B/KB/MB/GB 格式 | `fs.statSync()` 获取大小 |
| **MIME类型** | 根据文件扩展名自动识别 | `getMimeType()` 函数 |
| **修改时间** | 格式：yyyy-MM-dd HH:mm:ss | `fs.statSync()` 获取修改时间 |
| **可预览状态** | 通过 `canPreview` 接口检测 | `filePreview.canPreview()` API |
| **可读状态** | 文件是否具有读权限 | `fs.accessSync()` 检查读权限 |
| **可写状态** | 文件是否具有写权限 | `fs.accessSync()` 检查写权限 |

### 3. 交互设计
- **模态对话框**：居中显示，背景半透明遮罩
- **智能按钮状态**：
  - **立即预览按钮**：文件可预览时启用（蓝色），不可预览时置灰禁用
  - **取消按钮**：始终可用，点击关闭对话框
- **关闭方式**：
  - 点击"取消"按钮
  - 点击对话框外部区域
  - 按返回键
- **视觉反馈**：
  - 可预览/可读/可写状态用绿色（是）和红色（否）区分
  - 文件路径过长时自动截断显示
  - 文件大小和修改时间格式化显示

## 技术实现

### 核心组件：FileInfoDialog.ets

#### 1. 文件信息接口
```typescript
export interface FileInfo {
  fileName: string;           // 文件名
  filePath: string;           // 完整路径
  fileSize: number;           // 文件大小（字节）
  mimeType: string;           // MIME类型
  lastModified: number;       // 修改时间（时间戳）
  canPreview: boolean;        // 是否可预览
  readable: boolean;          // 是否可读
  writable: boolean;          // 是否可写
}
```

#### 2. 工具函数
- **`formatFileSize(bytes: number): string`**：格式化文件大小（B/KB/MB/GB）
- **`formatTime(timestamp: number): string`**：格式化时间戳为 yyyy-MM-dd HH:mm:ss
- **`getFileInfo(filePath: string): Promise<FileInfo>`**：获取文件完整信息
- **`getMimeType(fileName: string): string`**：根据扩展名获取MIME类型

#### 3. 对话框组件
- **`FileInfoDialogContent`**：使用 `@Component` 装饰的自定义对话框内容组件
- **`fileInfoDialogBuilder`**：使用 `@Builder` 装饰的对话框构建器
- **`showFileInfoDialog()`**：显示文件信息对话框的主函数
- **`showFileInfoDialogByPath()`**：通过文件路径显示对话框的便捷函数

#### 4. 对话框管理
- 使用 `UIContext.getPromptAction().openCustomDialog()` 打开自定义对话框
- 支持自动关闭上一个对话框（避免重复打开）
- 保存对话框ID用于后续关闭操作

### 集成方式

#### 1. 文件列表集成（FileListView.ets）
```typescript
// 处理文件点击
private async handleFileClick(file: FileItem): Promise<void> {
  if (this.isMultiSelectMode) {
    // 多选模式下，切换选中状态
    this.toggleFileSelection(file);
  } else {
    // 普通模式下，显示文件信息对话框
    await this.showFileInfo(file);
  }
}

// 显示文件信息对话框
private async showFileInfo(file: FileItem): Promise<void> {
  if (!this.context) return;
  
  await showFileInfoDialogByPath(
    this.context,
    file.path,
    () => {
      // 确认预览回调
      this.previewFile(file);
    },
    () => {
      // 取消回调
      console.log('用户取消预览');
    }
  );
}
```

#### 2. 上下文菜单集成
```typescript
// 显示文件上下文菜单（长按触发）
private async showFileContextMenu(file: FileItem): Promise<void> {
  const result = await promptAction.showActionMenu({
    title: file.name,
    buttons: [
      { text: '详情', color: '#007DFF' },
      { text: '多选', color: '#333333' },
      { text: '取消', color: '#999999' }
    ]
  });
  
  if (result.index === 0) {
    // 详情
    await this.showFileInfo(file);
  } else if (result.index === 1) {
    // 多选
    this.enterMultiSelectMode();
    this.toggleFileSelection(file);
  }
}
```

#### 3. 多选模式集成
```typescript
// 批量显示文件详情
private async batchShowDetails(): Promise<void> {
  if (this.selectedFiles.length === 0) return;
  
  // 显示第一个选中文件的详情
  const file = this.selectedFiles[0];
  
  // 如果选择了多个文件，显示提示
  if (this.selectedFiles.length > 1) {
    promptAction.showToast({
      message: `显示第1个文件的详情（共${this.selectedFiles.length}个）`
    });
  }
  
  await this.showFileInfo(file);
}
```

## 文件结构变化

### 新增文件
```
entry/src/main/ets/components/FileInfoDialog.ets
```

### 修改文件
1. **FileListView.ets**：
   - 导入 `showFileInfoDialogByPath` 函数
   - 添加 `showFileInfo()` 方法处理文件信息显示
   - 修改 `handleFileClick()` 逻辑，单击文件时显示详情对话框
   - 在上下文菜单中添加"详情"选项
   - 在多选操作栏中添加"详情"按钮

2. **FileList.ets**：
   - 新增 `getMimeType()` 函数用于MIME类型识别
   - 完善文件信息获取逻辑

3. **Index.ets**：
   - 保持原有预览功能，新增的文件列表模块已集成详情预览

## API使用

### 使用的HarmonyOS Kit
1. **`@kit.CoreFileKit`**：
   - `fileIo`：文件读写操作（`statSync`, `accessSync`）
   - `fileUri`：URI转换（`getUriFromPath`）

2. **`@kit.PreviewKit`**：
   - `filePreview`：文件预览功能（`canPreview`, `openPreview`）

3. **`@kit.ArkUI`**：
   - `promptAction`：显示对话框和Toast
   - `UIContext`：UI上下文管理
   - `ComponentContent`：组件内容封装

4. **`@kit.BasicServicesKit`**：
   - `BusinessError`：错误处理

### 关键API调用
```typescript
// 检查文件是否可预览
const canPreview = await filePreview.canPreview(hostContext, uri);

// 打开自定义对话框
context.getPromptAction().openCustomDialog({
  builder: () => {
    return fileInfoDialogBuilder(params);
  },
  alignment: DialogAlignment.Center,
  maskColor: 0x33000000,
  autoCancel: true
});

// 检查文件访问权限
const readable = fs.accessSync(filePath, AccessMode.READ);
const writable = fs.accessSync(filePath, AccessMode.WRITE);
```

## 用户体验优化

### 1. 性能优化
- **异步加载**：文件信息获取和预览检查均为异步操作，避免阻塞UI
- **缓存管理**：对话框ID缓存，避免重复打开
- **错误处理**：完善的错误捕获和用户提示

### 2. 视觉设计
- **响应式布局**：对话框宽度80%，适应不同屏幕尺寸
- **颜色编码**：状态信息使用颜色区分（绿色/红色）
- **信息层次**：重要信息突出显示，次要信息使用灰色
- **交互反馈**：按钮状态实时反映文件可操作性

### 3. 无障碍支持
- **键盘导航**：支持返回键关闭对话框
- **屏幕阅读器**：合理的文本标签和语义化结构
- **触摸目标**：按钮尺寸符合最小触摸目标要求

## 使用场景

### 场景1：文件预览前确认
用户在文件列表中单击文件，首先看到文件详细信息，确认文件属性后再决定是否预览。

### 场景2：权限检查
用户可通过对话框快速查看文件的可读/可写状态，避免因权限问题导致操作失败。

### 场景3：批量操作参考
在多选模式下，用户可查看选中文件的详细信息，为批量操作提供参考。

### 场景4：文件管理
作为文件管理工具的一部分，提供完整的文件元数据查看功能。

## 与原有功能的关系

### 增强而非替代
- **原有功能保留**：原有的直接预览功能仍然存在
- **前置辅助**：文件信息详情作为预览的前置步骤
- **无缝集成**：用户可选择跳过详情直接预览，也可先查看详情再决定

### 功能互补
- **信息完整性**：预览功能关注内容展示，详情功能关注元数据展示
- **操作流程**：详情→预览的流程更符合用户认知
- **错误预防**：提前显示不可预览状态，避免无效操作

## 技术亮点

### 1. 模块化设计
- 对话框组件独立封装，可复用
- 工具函数与UI组件分离，便于维护
- 接口定义清晰，类型安全

### 2. 错误处理
- 文件访问失败时提供友好提示
- 对话框打开失败时降级处理
- 网络或权限问题时的优雅降级

### 3. 国际化支持
- 文本内容可提取为资源文件
- 日期时间格式符合本地化要求
- 文件大小单位支持国际化

### 4. 扩展性
- 可轻松添加新的文件属性字段
- 支持自定义对话框样式
- 可集成到其他文件管理场景

## 测试要点

### 功能测试
1. 单击不同格式文件（txt、jpg、pdf等）显示正确的MIME类型
2. 文件大小正确格式化显示
3. 修改时间正确格式化显示
4. 可预览状态正确检测和显示
5. 文件权限正确检测和显示

### 交互测试
1. 对话框可通过点击外部区域关闭
2. 对话框可通过返回键关闭
3. "立即预览"按钮状态正确（可预览时启用，不可预览时禁用）
4. 长按菜单中的"详情"选项正常工作
5. 多选模式下的"详情"按钮正常工作

### 性能测试
1. 大文件信息获取不阻塞UI
2. 对话框打开/关闭动画流畅
3. 内存使用合理，无泄漏

## 已知限制

### 1. 平台限制
- `filePreview.canPreview()` API可能在某些设备上受限
- 某些文件类型的MIME类型识别可能不准确
- 权限检查可能受系统安全策略影响

### 2. 性能考虑
- 大量文件同时检查预览状态可能影响性能
- 对话框频繁打开/关闭可能影响用户体验
- 大文件信息获取可能需要较长时间

### 3. 用户体验
- 对于熟悉文件的用户，可能觉得详情对话框多余
- 需要平衡信息展示的完整性和界面简洁性
- 移动设备上对话框可能占用较多屏幕空间

## 未来优化方向

### 1. 功能增强
- 添加文件缩略图预览
- 支持文件属性编辑（重命名、移动等）
- 添加文件分享功能
- 支持文件搜索和过滤

### 2. 性能优化
- 添加文件信息缓存机制
- 实现懒加载和虚拟滚动
- 优化对话框打开速度

### 3. 用户体验
- 添加动画过渡效果
- 支持深色主题
- 添加手势操作支持
- 优化移动端适配

---

*本功能已完整集成到项目中，所有代码均已通过编译测试，可直接使用。*